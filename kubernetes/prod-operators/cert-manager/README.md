# Cert-Manager
Deploy's helm chart for Cert-Manager
## Make Functions
- **helm**<br />
Install helm repo and creates a template.
- **dump**<br />
Dry run before applying it.
- **apply**<br />
Deploys the storageClass and the already created template.